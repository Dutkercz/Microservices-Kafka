spring:
  application:
    name: produtos
  datasource:
    url: jdbc:postgresql://localhost:5555/iprodutos
    username: postgres
    password: rootpassword
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081
